需要增加的接口
1.单独修改用户密码
2.修改除了密码以外的其他用户信息
3.根据用户的ID查找地址(参数直接提供用户id,而不是一个地址的对象)
4.查找可用的服务
    查找结果:返回含有司机电话 名字等信息的表
    条件:1.司机能提供服务
         2.司机的时间符合
         3.地址上看看需要怎么筛选.(不行再讨论)


上次讨论：
1.  Available Time
	1.表结构主要是用户id，开始时间和结束时间
	2.Update 的方法是先删除对应用户的所有时间，然后再插入所有的条目
	3.插入数据之前要判断是否有时间上的重叠，并提示用户修改(同时要在		Javascript中实现检测)
2.  Car
	1.增加DAO,设置车的属性(不含图片)
	2.增加DAO,设置车的图片(数据库只保存图片名称即可,扩展名统一用		jpg,不需要保存扩展名和路径,直接在业务逻辑中生成完整的图		片名字即可,图片名字根据时间和随机码生成,DAO中直接插入一		个string到数据库中即可)
3.  地址增加按用户id搜索

